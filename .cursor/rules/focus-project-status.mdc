---
description: Focus project status handoff — current branch, versions, what to do next (update when pausing)
alwaysApply: true
---

# Focus — Project status (memory / handoff)

**Last updated:** 2026-07-26 (PyPI `focus-hud` **0.3.5** published — tag `v0.3.5`)  
**Product:** Focus — evidence-only blast-radius HUD. PyPI `focus-hud`, CLI `focus`, IDE `extensions/vscode-focus`. Repo: `j0viane/focus`.

**Read this first in a new chat.** Update this file when pausing or before switching windows.

---

## Where we are right now

| Item | State |
|---|---|
| **Branch** | `main` |
| **PyPI** | **`focus-hud` 0.3.5** ([pypi.org/project/focus-hud](https://pypi.org/project/focus-hud/)) — tag `v0.3.5` |
| **Extension** | **0.5.15** — wait for open-file LLM on Audit Local; no def risk rail; single ℹ️ CodeLens |
| **LLM captions** | Opt-in; **`qwen2.5-coder:3b`**; **`MAX_LABEL_CHARS = 320`**; never on overlay |
| **Phase 4d** | **Thin slice on main** — more shapes deferred |
| **Cache** | Local pack-hash only — no Redis |

### Do next

1. Phase 4 leftovers: tiny-diff HUD, symbol-level downstream, Marketplace
2. Phase 5 — GitHub inline

### Standing

- No commit/push/PR unless asked
- Dogfood CLI: `.venv/bin/focus` or `pip install focus-hud`
- Captions portable — no Focus-lore special cases
- LLM never on live overlay

---
description: Focus project status handoff — current branch, versions, what to do next (update when pausing)
alwaysApply: true
---

# Focus — Project status (memory / handoff)

**Last updated:** 2026-08-02 (open **#31**; `main` has tiny-diff `db82b18`; PyPI `focus-hud` **0.3.5**; extension **0.5.15**)  
**Product:** Focus — evidence-only blast-radius HUD. PyPI `focus-hud`, CLI `focus`, IDE `extensions/vscode-focus`. Repo: `j0viane/focus`.

**Read this first in a new chat.** Update this file when pausing or before switching windows.

---

## Where we are right now

| Item | State |
|---|---|
| **Checked-out branch** | `feat/agent-rules-upskilling-alignment` @ `2b0d59d` — tracks `origin/…` |
| **Open PR** | **[#31](https://github.com/j0viane/focus/pull/31)** — *docs(rules): align Focus gates with global upskilling home* — **OPEN**, mergeable, Focus HUD check **SUCCESS**. Rules/gitignore only (no runtime). One *(owner)* item: Reload Window / confirm Rules+Hooks in Customize |
| **`main` HEAD** | `db82b18` — *feat(triggers): pass-through for tiny diffs with low blast radius* (synced with `origin/main`) |
| **PyPI** | **`focus-hud` 0.3.5** — tag `v0.3.5` ([pypi.org/project/focus-hud](https://pypi.org/project/focus-hud/)). **Tiny-diff is on `main` source but not a new PyPI bump yet** — dogfood tiny-diff via `.venv/bin/focus` (editable checkout), not the published wheel alone |
| **Extension** | **0.5.15** — Audit Local waits for open-file LLM before first paint; no `def` risk rail; single ℹ️ CodeLens |
| **LLM captions** | Opt-in; default **`qwen2.5-coder:3b`**; `MAX_LABEL_CHARS = 320`; prompt rev `320-2sent`; never on overlay |
| **Phase 4c / 4d** | Caption labeler + thin ledger slice on main (#29); sweet-spot + quieter IDE chrome (#30) |
| **Phase 4b ROA** | **Tiny-diff HUD** on `main` (`db82b18`): ≤5 changed lines **and** &lt;2 downstream files **and** no path/Danger/shared seed → **pass-through** (no Mermaid). Constants: `TINY_DIFF_MAX_LINES`, `TINY_DIFF_MAX_DOWNSTREAM_FILES` in `src/focus/triggers/rules.py`; wired via `count_changed_lines` + `should_emit_diagram(..., changed_line_count=)` from `audit.py`. Docs: `docs/TRIGGERS.md`, `docs/ROADMAP.md` |
| **Cache** | Local pack-hash memory+disk only — **no Redis** |
| **Global binary** | Broken `uv tool` focus-hud uninstalled; dogfood via `.venv/bin/focus` or `pip install focus-hud` |
| **Marketplace** | **Held** until product feels solid / owner says go |
| **Agent teaching** | Canonical **only** private `cursor-rules` → `rules/upskilling.mdc` + hooks (`session-start-teach.sh`, `ask-before-git-publish.sh`). **No stop-hook recap** — append upskilling recap in the **same reply** (token save). No Focus `learning`/`mentorship` rule files. Focus gates point at upskilling; engineering §17 = owner-owned co-write |

### Recently shipped (product + process)

| When | What | Notes |
|---|---|---|
| 2026-08-02 | Tiny-diff → pass-through | `db82b18` on **`main` directly** — process mistake; lesson locked: product work = feature branch → PR. Dogfooded `/tmp/focus-tiny-dogfood` (tiny billing → pass-through; large/auth hub → full HUD). Tests green at ship time (~176) |
| 2026-08-02 | Upskilling / hooks (private `cursor-rules`) | `bbb626c` + follow-up on **`cursor-rules` `main`**: single teach home; sessionStart only for teach; **removed `stop-upskill-recap.sh`** (recap same-reply); publish hook allows main only for `cursor-rules` |
| 2026-08-02 | Focus rules alignment | Branch + **PR #31** (`2b0d59d` + handoff refresh): gates §9 dogfood / §10 feature-branch; teach → upskilling only; eng §17; detailed project-status memory |
| Earlier | #29 / #30 / 0.3.5 / 0.5.15 | LLM captions latency + 4d thin slice; caption sweet-spot + quieter IDE; README pin/honesty |

### Locked UX (do not regress without options table)

1. Captions: sweet-spot what/why; up to two short sentences; ≤320 chars; `prefer_polish_label`
2. Body edit wins over docstring for ℹ️ anchor (`_run_is_docstring_only`)
3. Drop IDE risk rail on `def` — implication stays in HUD/JSON/PR only
4. With `focus.llmCaptions`: wait for open-file LLM before first paint (no short deterministic flash); rest background
5. Never LLM on live overlay / save path
6. One `explanationLensTitle` CodeLens — Cursor joins same-line lenses with ` | `; no multi-lens wrap
7. Tiny + low blast → pass-through; path / Danger Zone / shared-layout seeds still force full HUD even on a one-line edit

### Dogfood notes

- Bake-off winner still `qwen2.5-coder:3b` for latency; quality mixed (chatty / confused) — quality pass is separate (options table when fresh)
- README mock: ℹ️ at the edit, risk in the HUD (`8ded2aa`)
- Workspace `.vscode/settings.json` may set `focus.llmCaptions: true` (gitignored)
- Tiny-diff dogfood fixture idea: small edit under `tests/fixtures/glass_box/billing/service.py` pattern / `/tmp/focus-tiny-dogfood`
- Prefer `.venv/bin/focus` for anything newer than PyPI 0.3.5 (includes tiny-diff on this checkout’s `main`)

### Do next

1. **Merge #31** when owner OK (Reload Window check) — then return checkout to `main` / delete feature branch
2. **Symbol-level downstream** (next Phase 4 product item) — feature branch first; options table if it touches graph topology / ROA
3. More **4d ledger** shapes; **JSDoc/TSDoc/Typer** heuristics
4. Caption **quality** (prompt/pack vs 7B) — options table; measure-first still holds
5. **Phase 5** — GitHub inline diff annotations
6. **Marketplace** — still held
7. Optional later: PyPI bump if tiny-diff should ship to installers; vulture/radon on `explain.py` only (~2.5k LOC concentration; not a rewrite)

### Standing (agent + owner)

- No commit/push/PR unless asked
- **Feature branch + PR** for Focus (and similar product repos) — never ship straight to `main` unless explicit `ALLOW_MAIN_COMMIT` (`focus-agent-gates.mdc` §10). Private **`cursor-rules` may use `main`**
- **Dogfood before commit/push** — real CLI/IDE on a concrete edit; show evidence; wait for owner OK (`focus-agent-gates.mdc` §9). Pytest ≠ product proof
- Own PR Test plan: automatable boxes `[x]` with evidence; leave only *(owner)* items
- README honesty on user-visible changes; honest about PyPI vs this checkout
- Dogfood CLI: `.venv/bin/focus` (editable) or published `focus-hud>=0.3.5`
- Captions must stay portable — no Focus-lore special cases
- LLM never on live overlay; packs only; never full files / full graph
- Teaching = **`upskilling.mdc` only** — expand acronyms; no-CS-school bar; recap what/why/connects/breaks **in the same reply**; **no interview one-liners** unless she asks for interview prep; **no stop-hook second turn**
- Git author for push: GitHub noreply `71949760+j0viane@users.noreply.github.com` (avoids GH007)

---
description: Focus project status handoff — current branch, versions, what to do next (update when pausing)
alwaysApply: true
---

# Focus — Project status (memory / handoff)

**Last updated:** 2026-07-16 (LLM captions merged; Tree-sitter crash-dialog fix in flight)  
**Product:** Focus — evidence-only blast-radius HUD. PyPI `focus-hud`, CLI `focus`, IDE `extensions/vscode-focus`. Repo: `j0viane/focus`.

**Read this first in a new chat.** Update this file when pausing or before switching windows.

---

## Where we are right now

| Item | State |
|---|---|
| **Branch** | `fix/js-treesitter-crash-dialog` (after #27 on `main`) |
| **Extension** | **0.5.9** — no auto-audit on window open; skip known Tree-sitter crash files |
| **LLM captions** | Merged (#27) — opt-in; `focus.llmCaptions` + Audit Local |
| **Autosave** | Deterministic only (no LLM) |

### Do next

1. Land crash-dialog PR → `./scripts/install-extension.sh` → Reload
2. Dogfood LLM captions with decision rubric (5–10 symbols)
3. Optional: `uv tool uninstall focus-hud` (broken global PATH binary)

### Standing

- No commit/push/PR unless asked
- Dogfood CLI: `.venv/bin/focus`

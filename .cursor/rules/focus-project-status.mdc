---
description: Focus project status handoff — current branch, versions, what to do next (update when pausing)
alwaysApply: true
---

# Focus — Project status (memory / handoff)

**Last updated:** 2026-07-26 (caption sweet-spot dogfood — uncommitted on `main`)  
**Product:** Focus — evidence-only blast-radius HUD. PyPI `focus-hud`, CLI `focus`, IDE `extensions/vscode-focus`. Repo: `j0viane/focus`.

**Read this first in a new chat.** Update this file when pausing or before switching windows.

---

## Where we are right now

| Item | State |
|---|---|
| **Branch** | `main` — **local uncommitted** caption UX work (see below); do not commit unless owner asks |
| **Extension** | **0.5.15** installed (dogfood) — one ℹ️ CodeLens (no multi-lens `|` join); Audit Local waits for open-file LLM before first paint |
| **LLM captions** | Opt-in; **`qwen2.5-coder:3b`**; cap **`MAX_LABEL_CHARS = 320`**; prompt ≤320 / up to two sentences; `_PROMPT_REV = "320-2sent"`; never on overlay |
| **IDE chrome** | **No risk rail** on `def` (implication → HUD only); ℹ️ at body edit (docstring hunks deferred when body exists) |
| **Cache** | Local pack-hash memory+disk only — **no Redis** (locked) |

### Tonight’s uncommitted slice (caption sweet-spot)

- Prompts + `prefer_polish_label` + 320-char budget + cache `prompt_rev`
- Body-over-docstring caption anchor (`_run_is_docstring_only`)
- Drop IDE risk rail; Option A: wait for open-file LLM on Audit Local
- Revert multi-lens wrap (Cursor joins with ` | `) → single CodeLens title
- Local (gitignored): `npm.exclude` for extension `package.json`; shell compile task

### Dogfood notes (2026-07-26)

- Long LLM captions on `cache.py` look good once Audit Local finishes (cache hits fast)
- CodeLens does **not** soft-wrap; full text is one line (click/hover for trust cues)
- Redis considered → rejected for now (cold path is model time, not cache lookup)

### Do next

1. **Commit/PR when owner asks** — caption sweet-spot + IDE chrome (separate from docs-only tweaks)
2. Optional: ROADMAP honesty (4d shipped); PyPI 0.3.5 publish
3. Optional later: caption quality bake-off vs 7B; Marketplace; Phase 5

### Standing

- No commit/push/PR unless asked
- Dogfood CLI: `.venv/bin/focus`; extension status bar should say **v0.5.15**
- Captions portable — no Focus-lore special cases
- LLM never on live overlay

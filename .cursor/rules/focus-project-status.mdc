---
description: Focus project status handoff — current branch, versions, what to do next (update when pausing)
alwaysApply: true
---

# Focus — Project status (memory / handoff)

**Last updated:** 2026-07-26 (#29 merged — Phase 4d + caption latency on main)  
**Product:** Focus — evidence-only blast-radius HUD. PyPI `focus-hud`, CLI `focus`, IDE `extensions/vscode-focus`. Repo: `j0viane/focus`.

**Read this first in a new chat.** Update this file when pausing or before switching windows.

---

## Where we are right now

| Item | State |
|---|---|
| **Branch** | `main` (clean; #29 merged) |
| **Extension** | **0.5.11** — progressive paint → LLM open-file (`--llm-path`) → full LLM; pack-hash cache + concurrency 8 |
| **LLM captions** | Opt-in; default model **`qwen2.5-coder:3b`** (fallback `qwen2.5-coder:7b`); no weak-gate; never on overlay |
| **Global binary** | Broken `uv tool` focus-hud **uninstalled**; only `.venv/bin/focus` remains |
| **Phase 4d** | **Shipped on main** — ledger `edit_facts.py` + orphan packs + LLM polish + line-of-sight |

### Dogfood notes

- Bake-off winner: `qwen2.5-coder:3b` (~8.8s / 10 packs, 8/10 validate) vs 7b slower / 3b non-coder worse
- Visible-file-first CLI: `focus audit --local --llm-captions --llm-path <rel>`
- IDE dogfood on `cache.py` (2026-07-26, #29 owner Test plan): CRITICAL rails + ℹ️ + proven hover — progressive paint OK; **ℹ️ quality still mixed** (chatty / confused) — quality pass is next
- Workspace `.vscode/settings.json`: `focus.llmCaptions: true` for IDE dogfood (gitignored)

### Bloat / size tooling (owner ask — memory only; not installed, not committed)

Deps are lean; perceived bloat is mostly **code concentration** (`explain.py` ~2591 LOC). When owner wants a pass later: vulture / radon / ruff / cloc / `uv tree` — prefer targeted `explain.py` look over drive-by rewrite.

### Do next

1. **Caption quality** (prompt/pack vs 7B vs tighter templates) — options table when fresh (STOP: LLM boundary)
2. Optional: refresh `docs/ROADMAP.md` Phase 4d status to “shipped on main” if still stale
3. Optional later: Marketplace polish; Phase 4 leftovers (symbol-level downstream, tiny-diff HUD); Phase 5 GitHub inline; vulture/radon on `explain.py`

### Standing

- No commit/push/PR unless asked
- Dogfood CLI: `.venv/bin/focus`
- Captions must stay portable — no Focus-lore special cases

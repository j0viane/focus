# Optional Focus config — copy to `.focus.toml` at your repo root.
# Missing file = defaults (fan_out_threshold = 2).

[focus]
# A file becomes a Danger Zone when this many other files import it directly.
fan_out_threshold = 2
